'use strict';

describe('myApp.twitterTarget module', function() {

  beforeEach(module('myApp.twitterTarget'));

  describe('twitterTarget controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var twitterTargetCtrl = $controller('twitterTargetCtrl');
      expect(twitterTargetCtrl).toBeDefined();
    }));

  });
});
